spring:
  application:
    name : "Test ZARA"
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:testZara
    username: "sa"
    password: " "
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        trace: true
        web-allow-others: false
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
    show-sql: true
